# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: C++ UT

on:
  workflow_dispatch:
  schedule:
    - cron: "0 18 * * *"
  pull_request:
    types: [ opened, reopened, synchronize ]
    paths:
      - 'src/main/cpp/**'
      - '.github/workflows/ut-cpp.yml'
      - '.github/workflows/scripts/ut-cpp/**'

concurrency:
  group: ${{ github.repository }}-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  ut-streaming-ubuntu24:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    env:
      HOST_CCACHE_DIR: "${{ github.workspace }}/.ccache"
    steps:
      - uses: actions/checkout@v4
      - name: Restore Ccache
        uses: actions/cache/restore@v4
        with:
          path: '${{ env.HOST_CCACHE_DIR }}'
          key: ut-cpp-ubuntu24-ccache
          restore-keys: |
            ut-cpp-ubuntu24-ccache
      - name: Run setup script / Build and run streaming test
        run: |
          docker run -v ${{ github.workspace }}:/velox4j -w /velox4j ubuntu:24.04 bash -c "
            export CCACHE_DIR=/velox4j/.ccache
            export CCACHE_MAXSIZE=3000M
            bash .github/workflows/scripts/ut-cpp/setup-ubuntu24.sh
            bash src/main/cpp/test-streaming.sh
          "
      - name: Save Ccache
        uses: actions/cache/save@v4
        with:
          path: '${{ env.HOST_CCACHE_DIR }}'
          key: ut-cpp-ubuntu24-ccache

  ut-ubuntu24:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    env:
      HOST_CCACHE_DIR: "${{ github.workspace }}/.ccache"
    steps:
      - uses: actions/checkout@v4
      - name: Restore Ccache
        uses: actions/cache/restore@v4
        with:
          path: '${{ env.HOST_CCACHE_DIR }}'
          key: ut-cpp-ubuntu24-ccache
          restore-keys: |
            ut-cpp-ubuntu24-ccache
      - name: Run setup script / Build and run UTs
        run: |
          docker run -v ${{ github.workspace }}:/velox4j -w /velox4j ubuntu:24.04 bash -c "
            export CCACHE_DIR=/velox4j/.ccache
            export CCACHE_MAXSIZE=3000M
            bash .github/workflows/scripts/ut-cpp/setup-ubuntu24.sh
            bash src/main/cpp/test.sh
          "
      - name: Save Ccache
        uses: actions/cache/save@v4
        with:
          path: '${{ env.HOST_CCACHE_DIR }}'
          key: ut-cpp-ubuntu24-ccache
